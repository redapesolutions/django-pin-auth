============
Installation
============

At the command line::

    $ easy_install django-pin-auth

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-pin-auth
    $ pip install django-pin-auth
